type Query {
  hello: String!
}

type Mutation {
  saveTextWithTitle(text: String!): SaveTextWithTitleResponse
}

type SaveTextWithTitleResponse {
  id: ID!
}

type AiResponse {
  id: ID!
  textHash: String!
  openAiID: ID!
  title: String!
  body: String!
}
